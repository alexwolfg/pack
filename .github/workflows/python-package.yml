name: Started with workflows

on:
  workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say smth
        run: echo "Hi there"
      - run: ls -l
      - uses: actions/checkout@v5
      - run: ls -l
